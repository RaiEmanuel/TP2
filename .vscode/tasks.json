{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "flex",
            "type": "shell",
            "command": "flex++ ./tp2.l && g++ -std=c++17 lex.yy.cc -o ./tp2",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}